{% extends 'JJWeddingBundle::manage.html.twig' %}

{% block main %}
    <div class="panel panel-default">
        <div class="panel-heading">
            <h4 class="panel-title"><span class="fa fa-list-alt"></span> New Block</h4>
        </div>
        <div class="panel-body">
            {{ form_start(form) }}

                <div>
                    {{ form_errors(form) }}
                </div>

                <div class="row">
                    <div class="form-group col-sm-6">
                        {{ form_label(form.title, 'Title') }}
                        {{ form_errors(form.title) }}
                        {{ form_widget(form.title, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group col-sm-6">
                        {{ form_label(form.location, 'Location') }}
                        {{ form_errors(form.location) }}
                        {{ form_widget(form.location, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
                <div class="row">
                    <div class="form-group col-sm-12">
                        {{ form_label(form.body, 'Body') }}
                        {{ form_errors(form.body) }}
                        {{ form_widget(form.body, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>

                <div class="row">
                    <div class="form-group col-sm-3 col-sm-offset-9">
                        {{ form_widget(form.submit, {'attr': {'class': 'btn btn-default form-control'}}) }}
                    </div>
                </div>

            {{ form_end(form) }}

        </div>
        <div class="panel-footer">
            <a href="{{ path('block') }}" class="btn btn-xs btn-danger" title="cancel">
                <span class="fa fa-times-circle"></span> Cancel</span>
            </a>
        </div>
    </div>
{% endblock %}
